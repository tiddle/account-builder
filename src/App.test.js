import React from 'react';
import { shallow } from 'enzyme';

import App from './App';

it('renders without crashing', () => {
    // const elem = shallow(<App />);
    // expect(elem.is('.App')).toBe(true);
});
